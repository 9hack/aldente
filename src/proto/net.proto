syntax = "proto2";

package proto;

message ServerMessage {
  optional bool status = 1;

  oneof message_type {
    string message = 2;
    Construct build_update = 3;
  }
}

message ClientMessage {
  optional bool status = 1;
  
  oneof message_type {
    string message = 2;
    Construct build_request = 3;
  }
}

message Construct {
  optional int32 type = 1;
  optional int32 x = 2;
  optional int32 z = 3;
}

// Below is currently unused.

message GameState {
  repeated GameObject objects = 1;
}

message GameObject {
  enum Type {
    PLAYER = 0;
    NPC = 1;
    TILE = 2;
  }

  message Location {
    repeated double x = 1 [packed = true];
    repeated double y = 2 [packed = true];
    repeated double z = 3 [packed = true];
    repeated double w = 4 [packed = true];
  }

  optional Type type = 1;
  optional Location location = 2;
}

message JoystickData {
  optional int32 id = 1;
  optional bool is_button = 2;
  optional int32 input = 3;
  optional int32 state = 4;
}

message Direction {
  enum Cardinal {
    NORTH = 0;
    EAST = 1;
    SOUTH = 2;
    WEST = 3;
  }
  optional Cardinal way = 1;
}